<div class="componente" data-componente="modal" data-modal-nombre="modal-post" data-modal-width="800" data-modal-title="Registrar">
	<div class="marco">
		<div class="componente" data-componente="formulario" data-formulario-nombre="formulario">
			<?php
			$form = $this->form;
			$form->setAttribute('data-ruta', $this->url($this->ruta, array('action' => 'agregar')));

			echo $this->form()->openTag($form);
			?>
			<div class="modal-body">
				<?php
				$layoutHorizontal = TwbBundle\Form\View\Helper\TwbBundleForm::LAYOUT_HORIZONTAL;

				// cnottitulo
				$input = $form->get('cnottitulo');
				$input->setAttributes(array(
					'data-bv-notempty'=> '',
					));
				$input->setOptions(array(
					'column-size' => 'lg-6',
					'label_attributes' => array(
						'class' => 'col-lg-3',
						),
					'twb-layout' => $layoutHorizontal,
					));
				echo $this->formRow($input);

				// cnotpietitulo
				$input = $form->get('cnotpietitulo');
				$input->setOptions(array(
					'column-size' => 'lg-9',
					'label_attributes' => array(
						'class' => 'col-lg-3',
						),
					'twb-layout' => $layoutHorizontal,
					));
				echo $this->formRow($input);

				// cnotrutaimgfull
				$input = $form->get('cnotrutaimgfull');
				$input->setAttributes(array(
					'data-bv-notempty'=> '',
					));
				$input->setOptions(array(
					'column-size' => 'lg-6',
					'label_attributes' => array(
						'class' => 'col-lg-3',
						),
					'twb-layout' => $layoutHorizontal,
					));
				echo $this->formRow($input);

				// dnotfecha
				$input = $form->get('dnotfecha');
				$input->setAttributes(array(
					'data-bv-notempty'=> '',
					));
				$input->setOptions(array(
					'column-size' => 'lg-3',
					'label_attributes' => array(
						'class' => 'col-lg-3',
						),
					'twb-layout' => $layoutHorizontal,
					));
				echo $this->formRow($input);

				// cnotcontenido
				$input = $form->get('cnotcontenido');
				$input->setAttributes(array(
					'data-bv-notempty'=> '',
					));
				$input->setOptions(array(
					'column-size' => 'lg-9',
					'label_attributes' => array(
						'class' => 'col-lg-3',
						),
					'twb-layout' => $layoutHorizontal,
					));
				echo $this->formRow($input);
				?>
				<div class="modal-footer">
					<button type="button" class="btn btn-default btn-modal cancelar" data-dismiss="modal">Cancelar</button>
					<?php
					// enviar
					$button = $form->get('enviar');
					$button->setOptions(array(
						'column-size' => 'sm-10 col-sm-offset-2',
						));
					echo $this->formButton($button);
					?>
				</div>
			</div>
			<?php echo $this->form()->closeTag($form); ?>
		</div>
	</div>
</div>